Host: 0.0.0.0
Port: 8080

# Admin panel credentials
Username: admin
Password: changeme

# Path to Next.js static export.
# Docker: Dockerfile copies admin-ui/out to ./web → use ./web
# Local dev: ../admin-ui/out
WebDir: ./web

MySQL:
  DSN: root:root@tcp(mysql:3306)/teamgram?charset=utf8mb4&parseTime=true

# Redis for instant cache invalidation after admin changes.
# Leave Host empty to skip Redis (changes will reflect after 15s TTL expires).
Redis:
  Host: redis:6379
  Pass: ""
